<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="Diplom1.Views.AbbRating" Title="Рейтинг тестов аббитуриентов">
    <ContentPage.Content>
        <Grid>
            <ListView BackgroundColor="White" x:Name="StudentsRating" IsPullToRefreshEnabled="True"
                  HasUnevenRows="True" Grid.Row="1" Margin="10,20"
                  Refreshing="StudentsRating_Refreshing"
                  ItemTapped="StudentsRating_ItemTapped"
                  ItemSelected="StudentsRating_ItemSelected"
                  ItemsSource="{Binding }">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <yummy:PancakeView 
                            Shadow="{yummy:ShadowMarkup Color=Black, Offset='10,10'}"
                            CornerRadius="15"
                            BackgroundColor="White"
                            >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Label Grid.Column="0" Grid.RowSpan="2" Text="{Binding ResultText}"/>
                                <Label Grid.Column="1" Grid.Row="0" Text="{Binding Fio}"/>
                                <Label Grid.Column="0" Grid.RowSpan="2" Text="{Binding Phone}"/>
                            </Grid>
                        </yummy:PancakeView>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>